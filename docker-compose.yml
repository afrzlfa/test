version: '2.2'
services:
    snake:
        image: 'afrzlfa/test:new'
        environment:
            - "PORT:3000"
        ports:
            - "80:3000"
        volumes:
            - ./:/var/www/html
